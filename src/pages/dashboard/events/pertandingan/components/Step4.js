import * as React from "react";

import { Card, CardBody } from "reactstrap";

export default function Step4({ eventDetail }) {
  console.log(eventDetail);
  return (
    <Card>
      <CardBody></CardBody>
    </Card>
  );
}
